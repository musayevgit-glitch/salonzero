{
  "buildCommand": "pnpm turbo run build --filter=@salonomia/database --filter=@salonomia/web...",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs"
}
