{
  "name": "@salonomia/database",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "lint": "eslint . --ignore-pattern generated",
    "typecheck": "prisma generate && tsc --noEmit",
    "test": "echo \"no tests yet: domain model lands in Phase 3\" && exit 0",
    "build": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0"
  }
}
